<Window x:Class="InventoryApp.Views.MovementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Движение товара" Height="280" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Товар -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Товар *"/>
            <ComboBox ItemsSource="{Binding Products}"
                      SelectedValue="{Binding ProductId, Mode=TwoWay}"
                      SelectedValuePath="Id"
                      DisplayMemberPath="Name"
                      Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Тип -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Тип операции *"/>
            <ComboBox ItemsSource="{Binding MovementTypes}"
                      SelectedItem="{Binding SelectedType, Mode=TwoWay}"
                      Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Количество -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Количество *"/>
            <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged,
                             ValidatesOnDataErrors=True}"
                     Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Комментарий -->
        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="Комментарий"/>
            <TextBox Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="5" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Зарегистрировать" Width="130" Margin="0,0,8,0"
                    Click="SaveButton_Click"/>
            <Button Content="Отмена" Width="80"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
