<Window x:Class="InventoryApp.Views.ProductDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Товар" Height="310" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Название -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Название *"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged,
                             ValidatesOnDataErrors=True}"
                     Margin="0,2,0,0"/>
        </StackPanel>

        <!-- SKU -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="SKU *"/>
            <TextBox Text="{Binding SKU, UpdateSourceTrigger=PropertyChanged,
                             ValidatesOnDataErrors=True}"
                     Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Начальный остаток -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Начальный остаток"/>
            <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged,
                             ValidatesOnDataErrors=True}"
                     Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Категория -->
        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="Категория *"/>
            <ComboBox ItemsSource="{Binding Categories}"
                      SelectedValue="{Binding CategoryId, Mode=TwoWay}"
                      SelectedValuePath="Id"
                      DisplayMemberPath="Name"
                      Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="5" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Сохранить" Width="90" Margin="0,0,8,0"
                    Click="SaveButton_Click"/>
            <Button Content="Отмена" Width="80"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
